query BookByPK($id: Int!) {
  books_by_pk(id: $id) {
    id
    slug
    title
    updated_at
    contributions {
      author {
        id
        name
      }
    }
    description
    image_id
    pages
    parent_book_id
    release_date
    rating
    ratings_count
    subtitle
  }
}
